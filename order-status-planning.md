# 訂單管理系統規劃文件

> 基於主管需求文件重新規劃的訂單系統架構

---

## 一、訂單資料結構定義

### 1.1 核心欄位（來自主管文件）

| 必須 | 欄位名稱 | 資料類型 | 說明 | 前端顯示 |
|------|---------|---------|------|---------|
| ✅ | 訂單編號 | String | `YYYYMMDD-XXXXX` + 轉單號（如有） | 顯示於卡片標題 |
| ✅ | 會員帳號 | String | 關聯會員資料 | 顯示會員名稱/頭像 |
| ✅ | 下單時間 | DateTime | 訂單建立時間 | "2025-11-05 14:30:00" |
| ✅ | 聊天連結 | URL | 打開與會員的聊天視窗 | 按鈕：「聯絡客戶」 |
| ✅ | 付款方式 | Enum | 目前都是「鑽石扣除」 | 顯示圖示 💎 |
| ✅ | 付款狀態 | Enum | 是否完成扣款（鑽石） | 「已付款」/「待付款」 |
| ✅ | 付款金額 | Number | 單筆結帳鑽石總額 | "💎 150" |
| ✅ | 預約時間 | DateTime | 何時要執行服務 | "2025-11-08 19:00" |
| ✅ | 預約時數 | Number | 該筆服務的時數 | "2 小時" |
| ✅ | 服務商藝名 | String | 提供服務的服務商 | 顯示藝名+頭像 |
| ✅ | 約會類型 | Enum | 私人約會的種類 | "咖啡約會"/"晚餐約會" |
| ✅ | 訂單狀態 | Enum | 目前的訂單階段 | 狀態徽章 |

### 1.2 系統欄位（追蹤用）

| 欄位名稱 | 資料類型 | 說明 |
|---------|---------|------|
| 建立時間 | DateTime | 註冊時間 |
| 建立人員 | String | 系統/會員/管理員 |
| 更新時間 | DateTime | 最後更新時間 |
| 更新人員 | String | 最後操作人 |

### 1.3 操作權限

| 角色 | 可用操作 |
|------|---------|
| 會員 | 退單（按鈕） |
| 服務商 | 退單（按鈕） |
| 管理員 | 取消退單（後台） |

---

## 二、訂單狀態定義與流程

### 2.1 狀態類型

| 狀態代碼 | 狀態名稱 | 顏色標記 | 說明 |
|---------|---------|---------|------|
| `pending` | 待確認 | 🟡 黃色 | 訂單剛建立，等待服務商確認 |
| `confirmed` | 待執行 | 🔵 藍色 | 服務商已確認，準備執行 |
| `in_progress` | 進行中 | 🟣 紫色 | 服務正在進行中 |
| `completed` | 已結束 | 🟢 綠色 | 服務已完成 |
| `cancelled` | 取消 | ⚫ 灰色 | 訂單被取消（無退款） |
| `refunded` | 退單 | 🔴 紅色 | 訂單退單（由後台處理退款） |

### 2.2 狀態流轉圖

```
待確認 (pending)
    ↓
    ├─→ 待執行 (confirmed)
    │       ↓
    │   進行中 (in_progress)
    │       ↓
    │   已結束 (completed)
    │
    ├─→ 取消 (cancelled)
    │
    └─→ 退單 (refunded) ← 任何階段都可退單
```

---

## 三、角色與操作權限矩陣

### 3.1 會員（Member）

| 當前狀態 | 可用操作 | 按鈕文字 | 說明 |
|---------|---------|---------|------|
| 待確認 | 退單 | 「我要退單」 | 填寫退單表單，送出給後台處理 |
| 待執行 | 退單 | 「我要退單」 | 填寫退單表單，送出給後台處理 |
| 進行中 | 確認開始 | 「確認開始」 | 捕捉服務開始時間 |
| 進行中 | 退單 | 「我要退單」 | 填寫退單表單，送出給後台處理 |
| 進行中 | 確認結束 | 「確認結束」 | 捕捉服務結束時間 |
| 已結束 | 確認結果 | 「確認結果」 | 10分鐘內需確認，否則自動確認 |
| 取消 | - | - | 無可用操作 |
| 退單 | - | - | 無可用操作 |

### 3.2 服務商（Service Provider）

| 當前狀態 | 可用操作 | 按鈕文字 | 說明 |
|---------|---------|---------|------|
| 待確認 | 確認訂單 | 「確認接單」 | 確認可以提供服務，狀態改為「待執行」 |
| 待確認 | 退單 | 「我要退單」 | 無法提供服務時退單 |
| 待執行 | 確認收到 | 「確認收到」 | 確認收到訂單資訊 |
| 待執行 | 退單 | 「我要退單」 | 申請退單 |
| 進行中 | 確認到場 | 「確認人到場」 | 確認會員已到達約會地點 |
| 進行中 | 退單 | 「我要退單」 | 申請退單 |
| 已結束 | - | - | 無可用操作 |
| 取消 | - | - | 無可用操作 |
| 退單 | - | - | 無可用操作 |

### 3.3 管理員（Admin）- 後台操作

| 操作 | 權限 | 說明 |
|------|------|------|
| 取消退單 | ✅ | 可直接取消會員/服務商的退單申請 |
| 強制變更狀態 | ✅ | 可手動修改訂單狀態 |
| 查看所有訂單 | ✅ | 不受角色限制 |
| 處理退款 | ✅ | 審核退單申請並處理鑽石退還 |

---

## 四、前端頁面設計規劃

### 4.1 訂單列表頁（會員視角）

**位置：** [`default.html`](default.html:1146) - 「訂單」分頁

#### 分頁標籤設計（類似蝦皮）

```
┌─────────────────────────────────────────────────────────┐
│ [全部] [待確認] [待執行] [進行中] [已結束] [取消] [退單]   │
└─────────────────────────────────────────────────────────┘
```

#### 單一訂單卡片設計

```
┌──────────────────────────────────────────────────────────┐
│ 訂單編號：20251105-00123                   [待確認 🟡]     │
│ ─────────────────────────────────────────────────────── │
│ 👤 服務商：安娜寶貝                                         │
│ 📅 預約時間：2025-11-08 (五) 19:00                         │
│ ⏱️  預約時數：2 小時                                        │
│ 🎭 約會類型：浪漫晚餐約會                                    │
│ 💰 付款金額：💎 150 鑽石                                    │
│ 💳 付款狀態：✅ 已付款                                      │
│ 📍 約會地點：台北市信義區                                    │
│ ─────────────────────────────────────────────────────── │
│ 📝 下單時間：2025-11-05 14:30                             │
│ ─────────────────────────────────────────────────────── │
│                                                          │
│ [💬 聊天]  [❌ 我要退單]                                   │
└──────────────────────────────────────────────────────────┘
```

### 4.2 接單列表頁（服務商視角）

**位置：** [`default.html`](default.html:1729) - 「接單」分頁

#### 單一接單卡片設計

```
┌──────────────────────────────────────────────────────────┐
│ 訂單編號：20251105-00123                   [待執行 🔵]     │
│ ─────────────────────────────────────────────────────── │
│ 👤 會員：陳小明                                            │
│ 📅 預約時間：2025-11-08 (五) 19:00                         │
│ ⏱️  預約時數：2 小時                                        │
│ 🎭 約會類型：浪漫晚餐約會                                    │
│ 💰 收入金額：💎 120 鑽石（扣除平台手續費）                    │
│ 📍 約會地點：台北市信義區                                    │
│ ─────────────────────────────────────────────────────── │
│ 📝 下單時間：2025-11-05 14:30                             │
│ ─────────────────────────────────────────────────────── │
│                                                          │
│ [💬 聊天]  [✅ 確認收到]  [❌ 我要退單]                      │
└──────────────────────────────────────────────────────────┘
```

### 4.3 退單申請彈窗（Modal）

```
┌──────────────────────────────────────┐
│  申請退單                        [✕]  │
├──────────────────────────────────────┤
│                                      │
│  訂單編號：20251105-00123            │
│  服務商/會員：安娜寶貝                 │
│                                      │
│  退單原因：                           │
│  ┌────────────────────────────────┐ │
│  │ [請選擇退單原因 ▼]              │ │
│  └────────────────────────────────┘ │
│  選項：                               │
│  - 服務商無法提供服務                  │
│  - 時間無法配合                       │
│  - 會員臨時有事                       │
│  - 其他原因                          │
│                                      │
│  詳細說明：                           │
│  ┌────────────────────────────────┐ │
│  │                                │ │
│  │  請詳細說明退單原因...            │ │
│  │                                │ │
│  └────────────────────────────────┘ │
│                                      │
│  ⚠️  注意事項：                       │
│  • 退單申請將送交後台審核              │
│  • 審核通過後鑽石將退回您的帳戶         │
│  • 惡意退單可能影響信用評分            │
│                                      │
├──────────────────────────────────────┤
│          [取消]      [送出申請]        │
└──────────────────────────────────────┘
```

---

## 五、特殊功能設計

### 5.1 進行中狀態 - 時數捕捉

當訂單進入「進行中」狀態時，需要記錄服務時間：

```
┌──────────────────────────────────────┐
│ 服務進行中                            │
├──────────────────────────────────────┤
│ ⏰ 服務開始時間：19:00                 │
│ ⏱️  已進行時數：01:23                  │
│ 📍 確認地點：台北市信義區              │
├──────────────────────────────────────┤
│ [✅ 確認結束服務]                      │
└──────────────────────────────────────┘
```

**時數捕捉時機：**
- 會員點擊「確認開始」→ 記錄開始時間
- 服務商點擊「確認人到場」→ 記錄到場時間
- 會員/服務商點擊「確認結束」→ 記錄結束時間，自動計算時數

### 5.2 已結束狀態 - 10分鐘自動確認

```
┌──────────────────────────────────────┐
│ 服務已結束，請確認結果                 │
├──────────────────────────────────────┤
│ ⏱️  服務時數：2 小時 05 分鐘            │
│ 💰 實際金額：💎 155 鑽石               │
│                                      │
│ ⚠️  系統將在 09:32 後自動確認          │
├──────────────────────────────────────┤
│ [✅ 確認結果]                          │
└──────────────────────────────────────┘
```

---

## 六、前端技術實作

### 6.1 訂單資料結構（JavaScript）

```javascript
// 單一訂單物件範例
const orderExample = {
  // 基本資料
  orderId: '20251105-00123',
  orderNumber: '20251105-00123',
  
  // 會員與服務商
  memberAccount: 'chen_ming',
  memberName: '陳小明',
  providerAccount: 'anna_baby',
  providerName: '安娜寶貝',
  
  // 時間相關
  orderTime: '2025-11-05T14:30:00+08:00',
  appointmentTime: '2025-11-08T19:00:00+08:00',
  appointmentHours: 2,
  
  // 付款相關
  paymentMethod: 'diamond', // 鑽石扣除
  paymentStatus: 'paid', // paid / pending
  paymentAmount: 150,
  
  // 服務資訊
  dateType: 'romantic_dinner', // 約會類型
  location: '台北市信義區信義路五段7號',
  
  // 狀態
  orderStatus: 'pending', // pending/confirmed/in_progress/completed/cancelled/refunded
  
  // 聊天
  chatLink: '/chat/room-12345',
  
  // 時數記錄（進行中狀態才有）
  serviceStartTime: null,
  arrivalTime: null,
  serviceEndTime: null,
  actualHours: null,
  
  // 系統欄位
  createdAt: '2025-11-05T14:30:00+08:00',
  createdBy: 'system',
  updatedAt: '2025-11-05T14:30:00+08:00',
  updatedBy: 'system'
};
```

### 6.2 狀態配置與按鈕顯示邏輯

```javascript
// 訂單狀態配置
const ORDER_STATUS_CONFIG = {
  pending: {
    label: '待確認',
    color: 'warning', // 黃色
    badge: 'kt-badge-warning',
    emoji: '🟡',
    member: [
      { action: 'openChat', label: '💬 聊天', class: 'kt-btn-secondary' },
      { action: 'requestRefund', label: '❌ 我要退單', class: 'kt-btn-danger' }
    ],
    provider: [
      { action: 'openChat', label: '💬 聊天', class: 'kt-btn-secondary' },
      { action: 'confirmOrder', label: '✅ 確認接單', class: 'kt-btn-success' },
      { action: 'requestRefund', label: '❌ 我要退單', class: 'kt-btn-danger' }
    ]
  },
  
  confirmed: {
    label: '待執行',
    color: 'info', // 藍色
    badge: 'kt-badge-info',
    emoji: '🔵',
    member: [
      { action: 'openChat', label: '💬 聊天', class: 'kt-btn-secondary' },
      { action: 'requestRefund', label: '❌ 我要退單', class: 'kt-btn-danger' }
    ],
    provider: [
      { action: 'openChat', label: '💬 聊天', class: 'kt-btn-secondary' },
      { action: 'confirmReceived', label: '✅ 確認收到', class: 'kt-btn-success' },
      { action: 'requestRefund', label: '❌ 我要退單', class: 'kt-btn-danger' }
    ]
  },
  
  in_progress: {
    label: '進行中',
    color: 'primary', // 紫色
    badge: 'kt-badge-primary',
    emoji: '🟣',
    member: [
      { action: 'openChat', label: '💬 聊天', class: 'kt-btn-secondary' },
      { action: 'confirmStart', label: '▶️ 確認開始', class: 'kt-btn-success' },
      { action: 'confirmEnd', label: '⏹️ 確認結束', class: 'kt-btn-primary' },
      { action: 'requestRefund', label: '❌ 我要退單', class: 'kt-btn-danger' }
    ],
    provider: [
      { action: 'openChat', label: '💬 聊天', class: 'kt-btn-secondary' },
      { action: 'confirmArrival', label: '📍 確認人到場', class: 'kt-btn-info' },
      { action: 'requestRefund', label: '❌ 我要退單', class: 'kt-btn-danger' }
    ]
  },
  
  completed: {
    label: '已結束',
    color: 'success', // 綠色
    badge: 'kt-badge-success',
    emoji: '🟢',
    member: [
      { action: 'confirmResult', label: '✅ 確認結果', class: 'kt-btn-success' }
    ],
    provider: []
  },
  
  cancelled: {
    label: '已取消',
    color: 'secondary', // 灰色
    badge: 'kt-badge-secondary',
    emoji: '⚫',
    member: [],
    provider: []
  },
  
  refunded: {
    label: '已退單',
    color: 'danger', // 紅色
    badge: 'kt-badge-danger',
    emoji: '🔴',
    member: [],
    provider: []
  }
};

// 根據訂單狀態和角色渲染按鈕
function renderOrderButtons(order, userRole) {
  const config = ORDER_STATUS_CONFIG[order.orderStatus];
  const buttons = config[userRole] || [];
  
  return buttons.map(btn => {
    return `
      <button class="kt-btn kt-btn-sm ${btn.class}" 
              onclick="handleOrderAction('${btn.action}', '${order.orderId}')">
        ${btn.label}
      </button>
    `;
  }).join('');
}
```

### 6.3 訂單操作函數

```javascript
// 訂單操作處理
function handleOrderAction(action, orderId) {
  const actionHandlers = {
    openChat: () => openChatWindow(orderId),
    requestRefund: () => showRefundModal(orderId),
    confirmOrder: () => confirmOrderAPI(orderId),
    confirmReceived: () => confirmReceivedAPI(orderId),
    confirmStart: () => captureStartTime(orderId),
    confirmEnd: () => captureEndTime(orderId),
    confirmArrival: () => captureArrivalTime(orderId),
    confirmResult: () => confirmResultAPI(orderId)
  };
  
  const handler = actionHandlers[action];
  if (handler) {
    handler();
  }
}

// 時數捕捉 - 開始時間
function captureStartTime(orderId) {
  const timestamp = new Date().toISOString();
  
  fetch('/api/orders/capture-time', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      orderId: orderId,
      actionType: 'start',
      timestamp: timestamp
    })
  })
  .then(response => response.json())
  .then(data => {
    if (data.success) {
      showNotification('✅ 已記錄服務開始時間', 'success');
      refreshOrderList();
    }
  });
}

// 已結束狀態 - 10分鐘自動確認倒數
function startAutoConfirmTimer(orderId, endTime) {
  const confirmDeadline = new Date(endTime).getTime() + (10 * 60 * 1000); // 10分鐘
  
  const timer = setInterval(() => {
    const now = new Date().getTime();
    const timeLeft = confirmDeadline - now;
    
    if (timeLeft <= 0) {
      clearInterval(timer);
      autoConfirmOrderAPI(orderId);
    } else {
      // 更新倒數顯示（格式：MM:SS）
      const minutes = Math.floor(timeLeft / 60000);
      const seconds = Math.floor((timeLeft % 60000) / 1000);
      document.getElementById('countdown-' + orderId).textContent = 
        `${String(minutes).padStart(2, '0')}:${String(seconds).padStart(2, '0')}`;
    }
  }, 1000);
}
```

---

## 七、API 端點規劃

### 7.1 訂單操作 API

```
GET    /api/orders                         # 取得訂單列表（會員/服務商）
GET    /api/orders/:orderId                # 取得單一訂單詳情
POST   /api/orders/:orderId/confirm         # 服務商確認接單
POST   /api/orders/:orderId/confirm-received # 服務商確認收到
POST   /api/orders/:orderId/capture-time    # 捕捉時數（開始/到場/結束）
POST   /api/orders/:orderId/confirm-result  # 會員確認結果
POST   /api/orders/:orderId/request-refund  # 申請退單
POST   /api/orders/:orderId/cancel          # 取消訂單
```

### 7.2 請求/回應格式範例

#### 申請退單 API

**Request:**
```json
POST /api/orders/20251105-00123/request-refund

{
  "reason": "時間無法配合",
  "message": "臨時有事無法參加，非常抱歉",
  "requestedBy": "member", // member / provider
  "timestamp": "2025-11-05T16:30:00+08:00"
}
```

**Response:**
```json
{
  "success": true,
  "message": "退單申請已送出，等待後台審核",
  "data": {
    "refundRequestId": "RF-20251105-001",
    "orderId": "20251105-00123",
    "status": "pending",
    "estimatedProcessTime": "1-2個工作天"
  }
}
```

#### 捕捉時數 API

**Request:**
```json
POST /api/orders/20251105-00123/capture-time

{
  "actionType": "start", // start / arrival / end
  "timestamp": "2025-11-08T19:05:00+08:00",
  "location": {
    "lat": 25.0330,
    "lng": 121.5654,
    "address": "台北市信義區信義路五段7號"
  }
}
```

**Response:**
```json
{
  "success": true,
  "message": "已記錄服務開始時間",
  "data": {
    "orderId": "20251105-00123",
    "serviceStartTime": "2025-11-08T19:05:00+08:00",
    "elapsedMinutes": 0
  }
}
```

---

## 八、資料庫設計

### 8.1 訂單主表（orders）

```sql
CREATE TABLE orders (
  id INT PRIMARY KEY AUTO_INCREMENT,
  order_number VARCHAR(20) UNIQUE NOT NULL COMMENT '訂單編號 YYYYMMDD-XXXXX',
  transfer_number VARCHAR(20) COMMENT '轉單號（對應舊資料）',
  
  -- 會員與服務商
  member_account VARCHAR(50) NOT NULL COMMENT '會員帳號',
  provider_account VARCHAR(50) NOT NULL COMMENT '服務商藝名',
  
  -- 時間相關
  order_time DATETIME NOT NULL COMMENT '下單時間',
  appointment_time DATETIME NOT NULL COMMENT '預約時間',
  appointment_hours DECIMAL(4,2) NOT NULL COMMENT '預約時數',
  
  -- 付款相關
  payment_method ENUM('diamond') DEFAULT 'diamond' COMMENT '付款方式',
  payment_status ENUM('paid', 'pending', 'refunded') DEFAULT 'pending' COMMENT '付款狀態',
  payment_amount INT NOT NULL COMMENT '付款金額（鑽石）',
  
  -- 服務資訊
  date_type VARCHAR(50) NOT NULL COMMENT '約會類型',
  location TEXT COMMENT '約會地點',
  chat_link VARCHAR(255) COMMENT '聊天連結',
  
  -- 訂單狀態
  order_status ENUM('pending', 'confirmed', 'in_progress', 'completed', 'cancelled', 'refunded') 
    DEFAULT 'pending' COMMENT '訂單狀態',
  
  -- 時數記錄
  service_start_time DATETIME COMMENT '服務開始時間',
  arrival_time DATETIME COMMENT '到場確認時間',
  service_end_time DATETIME COMMENT '服務結束時間',
  actual_hours DECIMAL(4,2) COMMENT '實際服務時數',
  
  -- 系統欄位
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '建立時間',
  created_by VARCHAR(50) DEFAULT 'system' COMMENT '建立人員',
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新時間',
  updated_by VARCHAR(50) DEFAULT 'system' COMMENT '更新人員',
  
  INDEX idx_member (member_account),
  INDEX idx_provider (provider_account),
  INDEX idx_status (order_status),
  INDEX idx_appointment (appointment_time)
) COMMENT='訂單主表';
```

### 8.2 訂單操作記錄表（order_logs）

```sql
CREATE TABLE order_logs (
  id INT PRIMARY KEY AUTO_INCREMENT,
  order_id INT NOT NULL COMMENT '訂單ID',
  action_type VARCHAR(50) NOT NULL COMMENT '操作類型',
  action_by VARCHAR(50) NOT NULL COMMENT '操作人帳號',
  action_role ENUM('member', 'provider', 'admin', 'system') COMMENT '操作角色',
  message TEXT COMMENT '操作說明',
  timestamp DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '操作時間',
  
  FOREIGN KEY (order_id) REFERENCES orders(id),
  INDEX idx_order (order_id),
  INDEX idx_timestamp (timestamp)
) COMMENT='訂單操作記錄表';
```

### 8.3 退單申請表（refund_requests）

```sql
CREATE TABLE refund_requests (
  id INT PRIMARY KEY AUTO_INCREMENT,
  refund_number VARCHAR(20) UNIQUE NOT NULL COMMENT '退單編號 RF-YYYYMMDD-XXX',
  order_id INT NOT NULL COMMENT '訂單ID',
  order_number VARCHAR(20) NOT NULL COMMENT '訂單編號',
  
  requested_by VARCHAR(50) NOT NULL COMMENT '申請人帳號',
  requester_role ENUM('member', 'provider') NOT NULL COMMENT '申請人角色',
  
  reason VARCHAR(100) NOT NULL COMMENT '退單原因',
  message TEXT COMMENT '詳細說明',
  
  status ENUM('pending', 'approved', 'rejected') DEFAULT 'pending' COMMENT '審核狀態',
  
  processed_by VARCHAR(50) COMMENT '處理人（管理員）',
  processed_at DATETIME COMMENT '處理時間',
  admin_note TEXT COMMENT '管理員備註',
  
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '申請時間',
  
  FOREIGN KEY (order_id) REFERENCES orders(id),
  INDEX idx_order (order_id),
  INDEX idx_status (status)
) COMMENT='退單申請表';
```

---

## 九、實作步驟建議

### 階段一：基礎功能（1-2天）

1. ✅ 在 [`default.html`](default.html:1146) 訂單分頁加入狀態篩選標籤
2. ✅ 在訂單卡片底部加入操作按鈕區塊
3. ✅ 根據訂單狀態和用戶角色動態顯示按鈕
4. ✅ 建立退單申請彈窗 (Modal)

### 階段二：狀態管理（2-3天）

1. 實作訂單狀態變更 API
2. 前端整合狀態切換功能
3. 加入狀態變更記錄功能
4. 實作即時通知（WebSocket 或輪詢）

### 階段三：時數捕捉（2天）

1. 實作「確認開始」「確認到場」「確認結束」功能
2. 前端顯示服務進行時數
3. 後端計算實際服務時數
4. 前端顯示時數差異提醒

### 階段四：自動確認（1天）

1. 實作 10 分鐘倒數計時器
2. 前端顯示倒數提醒
3. 後端設置自動確認機制
4. 前後端雙重保險機制

### 階段五：後台管理（2-3天）

1. 建立退單審核頁面
2. 管理員處理退單功能
3. 鑽石退款機制
4. 訂單強制變更功能

---

## 十、注意事項與風險控管

### 10.1 安全性

- ✅ 前端驗證使用者角色，只顯示可用操作
- ✅ 後端 API 必須再次驗證權限
- ✅ 重要操作需要二次確認（如退單、取消）
- ✅ 防止惡意重複提交（加入防抖機制）

### 10.2 資料一致性

- ✅ 狀態變更需記錄完整 log
- ✅ 時數捕捉需要時間戳記驗證
- ✅ 自動確認需前後端雙重檢查
- ✅ 退款操作需事務（Transaction）保護

### 10.3 用戶體驗

- ✅ 操作後即時反饋（成功/失敗提示）
- ✅ 載入中狀態（Loading）
- ✅ 錯誤提示清楚易懂
- ✅ 行動裝置友善設計

### 10.4 效能優化

- ✅ 訂單列表分頁載入（每頁 20 筆）
- ✅ 狀態篩選使用前端快取
- ✅ 即時更新使用 WebSocket（避免頻繁輪詢）
- ✅ 圖片延遲載入（Lazy Loading）

---

## 十一、討論問題清單

### 需要確認的問題：

1. **訂單編號格式**
   - 轉單號如何顯示？是否需要在前端顯示？
   - 範例：`20251105-00123 (轉單: OLD-001)`

2. **付款方式**
   - 目前只有「鑽石扣除」，未來是否會新增其他付款方式？
   - 是否需要預留擴充空間？

3. **約會類型**
   - 有哪些約會類型選項？
   - 是否需要顯示圖示？

4. **退單審核**
   - 退單申請多久內需處理？
   - 鑽石退款比例是否固定 100%？
   - 是否有部分退款的情境？

5. **時數計算**
   - 如果實際時數超過預約時數，是否需要額外收費？
   - 時數差異多少以內算正常範圍？

6. **自動確認**
   - 10 分鐘是否固定？能否調整？
   - 自動確認後是否發送通知？

7. **聊天功能**
   - 聊天連結是跳轉到哪個頁面？
   - 是否需要在同一頁面開啟聊天視窗？

8. **服務商接單邏輯**
   - 服務商可以拒絕訂單嗎？還是只能退單？
   - 拒絕和退單的差異是什麼？

---

## 十二、下一步行動

### 請主管/團隊確認：

1. ✅ 以上規劃是否符合需求？
2. ✅ 訂單資料結構是否正確？
3. ✅ 操作流程是否合理？
4. ✅ 前端設計是否符合預期？

### 確認後可立即開始：

1. 第一階段：實作基礎分頁和按鈕顯示
2. 準備測試資料
3. 建立 API Mock（方便前端先行開發）

---

**文件版本：** v2.0  
**最後更新：** 2025-11-05  
**負責人：** 開發團隊  
**狀態：** 📋 等待確認